import fontawesome from `@fortawesome/fonstawesome`;
import faClock from `@fortawesome/fonstawesome-free-regular/faClock`;
fontawesome.config = {
    autoReplaceSvg: `nest`
}
fontawesome.library.add(faClock);